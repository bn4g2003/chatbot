import { and, eq } from "drizzle-orm"; import { db } from "@/lib/db"; import { apiCredentials } from "@/lib/db/schema"; import { encryptSecret } from "@/lib/crypto"; import { requireAdmin } from "@/lib/session"; import { credentialSchema } from "@/lib/validation";
export async function POST(request: Request) { try { await requireAdmin(); const input = credentialSchema.parse(await request.json()); await db.delete(apiCredentials).where(and(eq(apiCredentials.ownerType, "system"), eq(apiCredentials.provider, input.provider))); const [row] = await db.insert(apiCredentials).values({ ownerType: "system", provider: input.provider, encryptedKey: encryptSecret(input.apiKey), keyLastFour: input.apiKey.slice(-4) }).returning({ id: apiCredentials.id, keyLastFour: apiCredentials.keyLastFour }); return Response.json(row, { status: 201 }); } catch { return Response.json({ error: "Forbidden or invalid" }, { status: 403 }); } }
