import { z } from "zod"; import { db } from "@/lib/db"; import { categories, categoryTranslations } from "@/lib/db/schema"; import { requireAdmin } from "@/lib/session";
const schema = z.object({ slug: z.string().regex(/^[a-z0-9-]+$/), vi: z.string().min(2).max(100), en: z.string().min(2).max(100) });
export async function GET() { return Response.json({ categories: await db.query.categories.findMany() }); }
export async function POST(request: Request) { try { await requireAdmin(); const input = schema.parse(await request.json()); const row = await db.transaction(async (tx) => { const [category] = await tx.insert(categories).values({ slug: input.slug }).returning(); await tx.insert(categoryTranslations).values([{ categoryId: category.id, locale: "vi", name: input.vi }, { categoryId: category.id, locale: "en", name: input.en }]); return category; }); return Response.json(row, { status: 201 }); } catch { return Response.json({ error: "Forbidden or invalid" }, { status: 403 }); } }
